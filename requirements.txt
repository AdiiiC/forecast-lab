# ─── Core ────────────────────────────────────────────────────────────────
numpy>=1.26
pandas>=2.1
scipy>=1.11
plotly
scikit-learn>=1.4
pyyaml>=6.0
tqdm>=4.66
rich>=13.7
matplotlib>=3.8

# ─── Classical / statistical models ──────────────────────────────────────
statsmodels>=0.14
prophet>=1.1.5

# ─── ML & DL models ──────────────────────────────────────────────────────
lightgbm>=4.3
torch>=2.2

# ─── Calendar / holidays ─────────────────────────────────────────────────
holidays>=0.50

# ─── HPO + experiment tracking ───────────────────────────────────────────
optuna>=3.6
mlflow>=2.12

# ─── Optional: changepoint detection (Milestone 5) ───────────────────────
# Install if you want ruptures-based changepoints; CUSUM fallback works without it.
ruptures>=1.1

# ─── Optional: zero-shot foundation models (Milestone 2) ─────────────────
chronos-forecasting>=1.4
#timesfm==1.0 # only timesfm 1.0.0 is currently available on PyPI; install manually if needed

# ─── Optional: MLOps / serving / pipelines (Milestone 6) ─────────────────
fastapi>=0.110
uvicorn[standard]>=0.29
gunicorn>=21
streamlit>=1.33
joblib>=1.4
ray[default]>=2.10
kafka-python>=2.0
snowflake-connector-python>=3.7
google-cloud-bigquery>=3.20
apache-airflow>=2.9
prefect>=2.18

# ─── Dev / tests ─────────────────────────────────────────────────────────
pytest>=8
ruff>=0.4